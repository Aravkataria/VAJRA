services:
  - type: web
    name: vajra-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/server.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
